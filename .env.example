# plat-geo environment configuration

# Required
GEO_DATA_DIR=.data           # Directory containing GeoParquet and PMTiles data files

# Optional
GEO_PORT=8086                # Port for the geo server
GEO_HOST=0.0.0.0             # Host address to bind
GEO_LOG_LEVEL=info           # Logging level (debug, info, warn, error)
